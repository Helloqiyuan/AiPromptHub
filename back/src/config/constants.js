const USER_ROLES = {
  USER: 'user',
  ADMIN: 'admin',
  SUPER_ADMIN: 'super_admin',
};

const USER_STATUS = {
  ACTIVE: 'active',
  DISABLED: 'disabled',
};

const PROMPT_STATUS = {
  PUBLISHED: 'published',
  PENDING: 'pending',
  REJECTED: 'rejected',
  ARCHIVED: 'archived',
};

module.exports = {
  USER_ROLES,
  USER_STATUS,
  PROMPT_STATUS,
};

